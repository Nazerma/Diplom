[project]
name = "nazerva-ai-shopping-assistant"
version = "0.1.0"
description = "AI-assisted Telegram shopping assistant with a content-based recommender."
readme = "README.md"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
